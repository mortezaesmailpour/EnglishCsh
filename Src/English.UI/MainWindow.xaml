<Window x:Class="English.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        xmlns:local="clr-namespace:English.UI"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800" >
        <Window.DataContext>
                <local:MainViewModel />
        </Window.DataContext>
        <Grid>
                <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="1">
                        <Label Content = "UserName :"/>
                        <TextBox Text="{Binding UserName}"/>
                        <Label Content = "Password :"/>
                        <PasswordBox local:PasswordHelper.Attach="True" local:PasswordHelper.Password="{Binding Path=Password, Mode=OneWayToSource}" />
                        <Button Content="Login" Click="ButtonBase_OnClick" />
                        <Button Content="Login2" Command="{ Binding ClickCommand}" />
                        <Button Content="Regirser"></Button>
                        
                </StackPanel>
        </Grid>
</Window>
