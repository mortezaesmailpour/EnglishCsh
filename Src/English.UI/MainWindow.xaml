<Window x:Class="English.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        xmlns:local="clr-namespace:English.UI"
        xmlns:viewModels="clr-namespace:English.UI.ViewModels"
        mc:Ignorable="d"
        Title="MainWindow" Height="600" Width="380">
    <Window.Resources>
        <viewModels:MainViewModel x:Key="mainVM"/>
    </Window.Resources>

    <StackPanel Orientation="Vertical" DataContext="{Binding Source={StaticResource mainVM}}">

        <ComboBox HorizontalAlignment="Left"   
                  Margin="183,39,0,0"   
                  VerticalAlignment="Top"   
                  Width="120"   
                  ItemsSource="{Binding Path=Subjects}"    
                  SelectedItem="{Binding Path=SelectedSubject}"  
                  DisplayMemberPath="Name"/>
        <Button Content="Add" Command="{Binding AddCommand}"/>
    </StackPanel>

</Window>
